/** Must match QuizGame.Core.GameConstants (quiz API validation). */
export const QUIZ_MIN_QUESTION_COUNT = 10
export const QUIZ_MAX_QUESTION_COUNT = 50

/** Must match GameConstants.WinThresholdPercentage — streak / stats treat this as a "win". */
export const WIN_THRESHOLD_PERCENTAGE = 80
